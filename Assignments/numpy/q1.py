# 1. Create a one-dimensional NumPy array containing integers from 1 to 10. Compute the sum, mean, and product of the array.

import numpy as np
arr = np.arange(1,11)
print(arr)
print(np.sum(arr))
print(np.mean(arr))
print(np.prod(arr))
